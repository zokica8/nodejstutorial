'use strict';

let counter = 100; // a variable to keep track of how many times to run a loop

while (counter > 0) {
  // () - a condition that MUST be true in order to run a code block inside the loop!
  console.log(counter);
  counter--; // a MUST to avoid infinite loop!!!!
}
